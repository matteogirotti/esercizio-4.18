#include <stdio.h>

int main(void) {
    int num,i;
    printf("inserisci un numero intero: ");
    scanf("%d",&num);
    printf("i divisori di %d sono: ", num);
    for(int i=1;i<=num;i++) {
        if(num%i==0) {

        }
    }
    printf("%d",i);
    return 0;
}
